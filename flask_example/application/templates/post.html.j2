{% extends "base.html.j2"%}
{% block content %}
<div class="container">
    <h1> {{ post.title | title }} </h1>

    <div class="jumbotron">
        <small>{{ post.date_created | format_date }}</small>
        <p>{{ markdown(post.content) }}</p>

    </div>

</div>

{% endblock%}